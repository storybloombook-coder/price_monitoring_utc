# Price Monitor 5.0.6 — инструкция

## Salidzini в 5.0.6 — ручная CAPTCHA, автоматический перенос цен

1. После обновления сборки откройте `edge://extensions` или `chrome://extensions` и нажмите **Reload / Обновить** у **PriceMonitor v5 Marketplace Bridge**. Версия расширения должна стать **5.0.6**. Если оно ещё не установлено, загрузите распакованную папку `edge-extension` рядом с EXE. Расширение v4 — отдельное, его обновлять не нужно.
2. Запустите мониторинг нужных SKU с включённым Salidzini. При Action required нажмите **Open verification**, пройдите CAPTCHA в обычной вкладке и оставьте выдачу открытой. Минутного ограничения для ручного захвата нет: даже после завершения прогона страницу можно отправить.
3. На самой вкладке Salidzini нажмите значок расширения. Проверьте **Original monitoring SKU**: это исходная модель из таблицы, даже если поиск на сайте был сокращён. Нажмите **Send to PriceMonitor**. Захватывается открытая страница, без новой вкладки и без перезагрузки.
4. В расширении появится предпросмотр: продавец, полная цена, название и наличие. Снимите галочки с ошибочных предложений и нажмите **Save selected prices**. До подтверждения таблица приложения не изменяется. Предпросмотр привязан к указанному прогону; если он изменился, нажмите **Refresh preview** и проверьте результат заново.
5. Если есть несколько страниц, отправьте каждую. Повторный захват той же страницы заменяет только её предложения. После проверки последней страницы можно отметить **All pages and offers for this SKU reviewed**. Пока вся выдача не проверена, цены видны, но статус остаётся Action required / partial. Пропущенные предложения нельзя считать подтверждённым отсутствием магазина.
6. При отключении приложения снимок сохраняется локально в расширении и передаётся для предпросмотра после восстановления связи (повтор раз в 30 секунд). Подтверждённое сохранение также переживает обрыв связи, без повторной записи тех же цен. Максимум 5 снимков по 2 MB, с общим ограничением очереди. **Discard snapshot** удаляет локальный снимок, но не отменяет уже подтверждённую запись. После успешного сохранения сырой HTML удаляется из очереди; в базе остаются цены и история решения.
7. Таблица PriceMonitor обновляется после сохранения в течение примерно 5 секунд. Ваш каталог и старые прогоны не перезаписываются при обновлении программы. Ручной импорт не попадает в автоматический кеш цен.

Наличие берётся только из явной информации в карточке, иначе остаётся Unknown. Rde.lv не подменяется на Rde.lt/Rde.ee. CAPTCHA не решается автоматически; при изменении разметки или отсутствии распознанных карточек используйте ручной ввод. Проверка на настоящей разблокированной странице Salidzini нужна отдельно: локальные тесты не подтверждают доступность сайта в вашей браузерной сессии.

## Исправления 5.0.5

- Резервный поиск теперь включён по умолчанию на **всех трёх маркетплейсах**: Kaina24, Salidzini и Hinnavaatlus. Работает как при обычном сборе HTML, так и через расширение. Сначала `TCL + исходный SKU`; если совпадений нет — без одного последнего символа, затем без двух. Ограничение: минимум 3 символа, с буквами и цифрами. Исходный SKU в каталоге не изменяется.
- Сокращается только запрос, а не критерий подтверждения товара. Точное совпадение или известные региональные пары S45HE/S45H, S55HE/S55H позволяют принять цену. Другие похожие модели показываются в Action required со ссылками для ручной сверки, без автоматической подстановки цены.
- Исправлен пропуск Varle для S55HE на Kaina24: название S55H теперь распознаётся и в поисковых карточках, включая вторую страницу, и на странице сравнения. Старый кеш для этих пар на Kaina24/Salidzini пропускается; история не переписывается.
- При CAPTCHA, ошибке сети, нераспознанной разметке или ошибке чтения цены дополнительные сокращённые запросы не запускаются. Ограничения времени, паузы между запросами и запрет переходов на сайты магазинов сохранены. Для повторной проверки существующей строки используйте ↻ у SKU; старые результаты сами не меняются.

## Исправления 5.0.4

- Marketplaces и Shops свёрнуты при открытии приложения; в заголовках видны счётчики включённых источников. Раскрываются зелёной круглой кнопкой. Переключение магазина не сворачивает список.
- Рядом с каждым маркетплейсом есть `i`: наведение, фокус клавиатурой или нажатие показывают правила парсинга и его ограничения.
- RDE.lt (Lithuania) и RDE.ee (Estonia) — разные переключатели, фильтры и столбцы, включая Excel. Старый переключатель RDE относится к RDE.ee. Уже сохранённые предложения Rde.lt отображаются в новом столбце без повторного сбора цены.
- На Hinnavaatlus после подтверждённого отсутствия совпадения выполняются ещё до двух поисков: без последнего символа, затем без двух последних. Минимум — 3 символа, буквы и цифры. S45HE → S45H и S55HE → S55H — явно заданные региональные обозначения; исходный SKU не меняется, рядом с ценой указано `matched S45H` / `matched S55H`. Основание: TCL Italy использует названия HE и идентификаторы H на [странице S55H](https://www.tcl.com/it/it/soundbar/s55h) и [странице поддержки S45H](https://www.tcl.com/it/it/support-soundbar/model/s45h).
- Другие сокращённые названия — только кандидаты в Action required со ссылками для ручной сверки. Их цены не подставляются автоматически. Если все доступные поиски завершены и нет ни совпадений, ни кандидатов — Not found. Неизвестная разметка, CAPTCHA, сеть и остановка не равны отсутствию товара. Сокращение касается только Hinnavaatlus; остальные площадки продолжают точный поиск.
- Senukai: берётся опубликованная на маркетплейсе цена SMART NET / с картой, с пометкой `Loyalty price`. Скидка не вычисляется, сайт Senukai не открывается. Если маркетплейс не показывает цену с картой, она не выдумывается. Старый кеш Kaina24 с обычной ценой Senukai автоматически пропускается для новой проверки; история не переписывается.
- Переключатель Mode · cache перенесён в свёрнутые Advanced settings → Price cache. По умолчанию 4 часа; ранее выбранный режим сохраняется. Кеш нужен для скорости и снижения числа запросов. Для полного свежего прогона — Fresh check · no cache; ↻ у SKU также не использует кеш.
- Unverified означает «проверка неполная, вывода пока нет». Not found — завершённый поиск не нашёл модель; Not listed у магазина — магазин не указан в полностью проверенных предложениях маркетплейсов. Эти статусы не объединены.

## Исправления 5.0.3 — Kaina24

- В v5 перенесён рабочий профиль запроса и парсер карточек Kaina24. Поиск — `TCL + модель`, затем страница сравнения со всеми текущими продавцами. Расширение для доступных страниц не требуется.
- Цена, продавец и наличие читаются отдельно. Рассрочка, доставка, похожие модели, рекламные дубли и архив распроданных предложений не попадают в текущие цены. Для клубной цены используется явно указанная обычная цена; без неё предложение требует проверки.
- Все ссылки результата ведут на Kaina24. Ссылки `/ex/…`, перенаправляющие в магазины, сборщик не открывает.
- Сохранённая страница сравнения используется при следующей проверке; при её исчезновении запускается поиск. При неполной загрузке сохраняются найденные цены с Action required, при защите остаётся cooldown.
- Для проверки обновления выберите `Deep` либо ↻ рядом с моделью: старый прогон не пересчитывается автоматически. Папка сборки сохраняет имя `v5.0.0`, версия приложения — `5.0.3`; каталог и история остаются на месте.

## Исправления 5.0.2

`Smartech Shop` на Hinnavaatlus теперь распознаётся как магазин Smartech.ee и попадает в столбец Smartech. Исправление применяется также к старым и кешированным результатам при открытии прогона: повторный запрос цены не нужен. Цена, время проверки и исходное имя продавца не меняются. Для одноимённых продавцов в других странах сохраняются ограничения сопоставления.

## Исправления 5.0.1

- Повторный запуск EXE открывает страницу уже работающего приложения. При отказе обычного открытия браузера используется стандартный обработчик ссылок Windows. Журнал запуска: `var/v5/logs/launcher.log`.
- В Step 2 слева от `+ Add` появилась `Clear table` для каждого списка. С подтверждением переносит все активные и приостановленные строки выбранного списка в Trash, независимо от фильтров. История и результаты не удаляются. Очистка склада не останавливает мониторинг моделей. Во время проверки сначала нужен Hard stop.
- `Mode · cache` влияет только на возраст кеша: Quick 12 ч, Balanced 4 ч, Deep без кеша. Методы сбора одинаковые; защиту сайтов режим не отменяет.
- Если наличие не подтверждено, собранная цена всё равно видна в сводке с пометкой `reported price · availability unconfirmed` и ссылкой. Она не включается в Lowest in-stock.
- Сетевые ошибки отличены от CAPTCHA/Not found. При `Network connection failed` проверьте подключение/правила брандмауэра и запускайте EXE обычным двойным щелчком из папки, не из ограниченной среды тестирования.

## Запуск и сохранность v4

Запустите `PriceMonitor.exe` из папки `PriceMonitor-v5.0.0-windows-x64-portable`.
Интерфейс откроется по адресу http://127.0.0.1:8050. Если вкладка закрыта, откройте этот адрес снова.
Не переносите EXE отдельно от `_internal`. Windows x64, установка Python не требуется.
Закрытие вкладки не завершает локальный сервер. Для полного выхода перед обновлением завершите процесс PriceMonitor в Диспетчере задач (сверьте путь к папке v5).

v4 остаётся в своей старой папке и использует порт 8000. Данные v5 независимы:
`var/v5/catalog.sqlite3`, экспорт — `var/v5/exports`. Для резервной копии закройте приложение и скопируйте всю папку `var`.
Git-точка отката v4: тег `v4.2.4`. Исходный код v4 не изменён.

## Что изменилось

- Запросы только к Kaina24, Salidzini и Hinnavaatlus. Переходов сборщика к магазинам нет.
- Магазины — это фильтры продавцов в предложениях маркетплейсов. Их переключатели скрывают/показывают столбцы и не меняют общерыночные min/max.
- Min/max считаются среди всех захваченных продавцов каждого маркетплейса, а не только выбранных магазинов.
- Считаются обычные цены товара с подтверждённым наличием. Рассрочка и доставка не прибавляются. Предзаказ идёт отдельно. Если наличие не подтверждено, цена видна в раскрытии и в столбце магазина с `Availability unknown`, но не попадает в in-stock min/max.
- Срок доставки на странице сам по себе не доказывает фактическое наличие.
- Колонка магазина показывает минимальную доступную in-stock цену; если таких нет — минимальную из остальных с явным указанием наличия в раскрытии. Там же все предложения, маркетплейс, время и ссылка на страницу сравнения.

## 1. Загрузка моделей

Достаточно импортировать сток XLSX/XLSM: B — название, C — количество, D — себестоимость. Строка с названием в B и пустыми C/D обозначает склад.
Распознанные модели автоматически добавляются в мониторинг. Одна модель на нескольких складах проверяется один раз на каждом включённом маркетплейсе.
Нулевой остаток и исчезновение строки из следующего файла не выключают мониторинг. Намеренно поставленные паузы и корзина сохраняются.
Неоднозначные названия остаются `Unmatched`: исправьте Linked model в Edit. Неоднозначные модели не угадываются.
Дополнительный файл моделей необязателен: листы TV/SB/Monitors, колонка Model. Можно добавлять вручную.
Поисковый запрос имеет вид `TCL + модель`.

## 2. Проверка

Включите нужные маркетплейсы и нажмите Start monitoring. На SKU — максимум три проверки вместо отдельных проверок магазинов.
Внутри каждой проверки могут понадобиться запрос выдачи, карточки сравнения и страниц списка продавцов.
Разные маркетплейсы проверяются параллельно, внутри каждого — последовательно с паузой 3 секунды.
Hard stop прекращает текущую работу и сохраняет полученные результаты. Кнопка ↻ у модели обновляет только её.
Предыдущий прогон открывается под таблицей; новые изменения каталога не добавляют строки в старые прогоны.

Режимы: Balanced — полный автоматический результат не старше 4 часов; Quick — до 12 часов; Deep — без кеша цен.
Ручные решения и отрицательные результаты не используются как новые автоматические подтверждения. Старые решения показаны с датой.
История хранится до очистки пользователем и не означает актуальную цену.

## 3. Защищённые страницы и ручная работа

`Action required` — CAPTCHA, неизвестная разметка или неполные данные. Это не `Not found`.
При защите автоматические обращения к этому маркетплейсу приостанавливаются на 10 минут; остальные площадки продолжают работать.
Ручной ввод доступен сразу, ждать cooldown не нужно. Проверку можно повторить кнопкой Capture again.

1. Open verification открывает маркетплейс. CAPTCHA проходите самостоятельно.
2. Можно вставить ссылку именно на страницу сравнения/выдачи маркетплейса. Ссылки Varle, RDE и прочих магазинов в v5 не принимаются.
3. Без расширения внесите цену, продавца и наличие вручную через Save manual price.
4. В раскрытии min/max кнопка Add seller offer / review добавляет остальных продавцов. Edit offer исправляет выбранное предложение; Remove false match удаляет ложное совпадение только из этого прогона.
5. После просмотра всех предложений отметьте `I reviewed all seller offers…`. До этого min/max помечены `partial`: это границы только внесённых предложений, а не всего рынка.
6. Mark not found применяется ко всему SKU на выбранном маркетплейсе, а не к одному продавцу.

`Not listed` у магазина означает, что он не указан на проверенных страницах маркетплейсов; это не доказательство отсутствия товара в самом магазине.
`Unverified` — площадки ещё не дали достаточно данных для вывода о магазине.
Жёлтая цена — отклонение от других собранных цен не менее чем на 20%, повод проверить модель; не автоматическое удаление.

## Необязательное расширение v5

Приложение работает без расширения: доступные HTML-страницы собираются автоматически, защищённые обрабатываются вручную.
Для захвата открытых после CAPTCHA страниц установите отдельное расширение v5:

1. Откройте `edge://extensions`, включите Developer mode.
2. Load unpacked → папка `edge-extension` рядом с новым EXE.
3. В расширении адрес `http://127.0.0.1:8050`. Проверьте соединение.

У v5 другой идентификатор расширения; v4 можно оставить установленным. Не подключайте старое расширение к v5.
Расширение v5 читает только три маркетплейса. Во вкладке захвата переходы за пределы выбранного маркетплейса блокируются.
Оно не решает CAPTCHA, не меняет IP и не обходит защиту.
Смена разметки сайта может потребовать ручного ввода. Live-проверки подтвердили извлечение продавцов Hinnavaatlus и Kaina24. Salidzini ранее возвращал hCaptcha и в этом патче не менялся. Работоспособность автоматического сбора не гарантирует отсутствие защиты при следующих обращениях; после CAPTCHA захват через установленное расширение надо проверить в вашей сессии.

## Excel

Export table to Excel: сводная таблица, все предложения продавцов и отдельный лист статусов проверок.
Печать — альбомная A4, одна страница по ширине, несколько по высоте при необходимости.
Excel допускает только одну ссылку на ячейку, поэтому отдельная кликабельная строка для каждого предложения находится на листе All seller offers.

## Обновление и передача

Сборка: `./build-v5.ps1`. Для чистого ZIP без вашего каталога: `./build-v5.ps1 -Archive`.
При обновлении не удаляйте `var`. v4-папку не перезаписывайте.
